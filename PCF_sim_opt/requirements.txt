# Core Dependencies (Existing)
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
pyomo>=6.6.0
plotly>=5.14.0

# Optimization Solvers
# Note: IPOPT and GLPK need to be installed separately via system package manager
# For Ubuntu/Debian: sudo apt-get install coinor-libipopt-dev glpk-utils

# NEW: Phase 1 Dependencies (Sensitivity Analysis + Interactive Navigator)
SALib>=1.4.7              # Sobol sensitivity analysis
scipy>=1.11.0             # Interpolation, statistics, signal processing
scikit-learn>=1.3.0       # Gaussian Process for interpolation (optional)

# NEW: Phase 3 Dependencies (Stochastic Risk Quantification - optional)
chaospy>=4.3.0            # Polynomial Chaos Expansion for fast uncertainty quantification

# Development and Testing (Optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.0.0
